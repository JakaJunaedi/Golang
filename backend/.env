DATABASE_URL=host=localhost user=postgres password=postgres dbname=golang_nextjs port=5432 sslmode=disable

# Server configuration
PORT=8080

# JWT Secret (if you're using JWT)
JWT_SECRET=your_jwt_secret_key_here